<tr> 
	<td><%= @idx %></td>
	<td class="<%=bg_icon%>" style="white-space:nowrap"> <%= overall_score %> </td>		

	<td><%= @prediction_def.scan(/([^ ]+)/)[0][0] %></td>
	<td><%= @prediction_def.scan(/([^ ]+)(.*)/)[0][1] %></td>		
	<td><%= @nr_hits %></td>
	<% @validations.each do |item| %>	    
   	    <% if item.plot_files.length > 0 %>
	   	    <td>
	   	    	<button class="<%=item.validation_report.color%> my-btn-<%=item.validation_report.color%>" 
	  				onclick=showDiv('<%= toggle %>')>	  		  			 
					<%= item.validation_report.print %> 	
				</button>				 
  	    <% else %>
  		    <td class = <%=item.validation_report.color%>>			    
   				<%= item.validation_report.print %>    	        
   	    <% end %>  	        
		</td>
	<% end %>
	<td style="white-space:nowrap">...</td>
</tr>

<tr class="expand-child">
	<td  colspan=<%=validations.length + 6%>>
	<div id=<%=toggle%> style='display:none'>"
    <% @validations.each do |item| 
       item.plot_files.each do |plot_file| %>
       <img src=<%=plot_file.scan(/\/([^\/]+)$/)[0][0]%> alt="No plot" height=400>
    <% end end %>
</div></td></tr>


